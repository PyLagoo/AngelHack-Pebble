with our pebbleapp you can share data just like name contact id etc with people on same geolocation.
This happens via this android app in cojugaton with cloudpebble.we sent the loction to firebase server and fetch the data stored in others app.


furthur updates:(not completed on time):
-Sentiment through speech recognition(by HavenoDemand apis)
-Following our main app, broadcasting/receiving sentiment of personal twitter handles via recent tweets

